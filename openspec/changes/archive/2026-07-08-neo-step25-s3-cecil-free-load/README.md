# neo-step25-s3-cecil-free-load

S3-2 TRUE COMPLETION: Cecil-free AppDomain load -- load a .neo into an AppDomain that has NOT Cecil-loaded the types (the true AOT deployment scenario). Install the S3-partial ILType rebuild on a live type.
