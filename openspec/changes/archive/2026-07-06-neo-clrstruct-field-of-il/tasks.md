## 1. Reproducer + dump-gate (BEFORE designing the fix — the F-6 / F-MAJ-1 discipline)

- [x] 1.1 Build the CLI (`dotnet build ILRuntimeTestCLI/ILRuntimeTestCLI.csproj -c Debug_Neo`) + TestCases (`dotnet build TestCases/TestCases.csproj -c Debug`); confirm 0 errors. Run the `NeoStep` smoke; confirm 186/186 green (environment-healthy baseline).
- [x] 1.2 Construct the minimal F-10 reproducer: an IL `class C { TestVector3NoBinding f; }` (a CLR-struct field), `C c = new C(); c.f = new TestVector3NoBinding(10f,20f,30f);` then take `ref c.f` via a helper that passes the field by value to a CLR method summing its fields (the byref deref flows through `CopyNeoCallArguments` → `NeoMarshalByrefFieldToSlot`). Confirm it FAILS on HEAD with `IndexOutOfRangeException` (or silent wrong result if the layout accident makes `Primitives` long enough — if so, shrink the IL class so `Primitives.Length` < the byref's `PrimitiveOffset + sz` to force the OOB).
- [x] 1.3 Dump the ILType field layout for the reproducer's IL class: `TotalPrimitiveSize`, `TotalReferenceCount`, and the CLR-struct field's `PrimitiveOffset` + `ReferenceOffset`. Confirm the field's `PrimitiveOffset` points PAST `Primitives` (the layout accident) and `ReferenceOffset` is where the boxed struct lives (`ManagedObjects[ReferenceOffset]`).
- [x] 1.4 Dump the JIT body of the reproducer: confirm the `ldflda &c.f` opcode stamps `Operand2 = field.PrimitiveOffset` (the stale offset). Add a temporary `Console.WriteLine` in the runtime `Ldflda` arm (`ILIntepreter.Neo.cs:1025-1092`) + in `NeoMarshalByrefFieldToSlot` (`:376+`) to print `(objIdx, off, sz, target.GetType())` for the F-10 path; confirm `target is ILTypeInstance` and `off = PrimitiveOffset` (stale). Remove the diagnostic before completing.
- [x] 1.5 Confirm the blast-radius assessment: `Stfld_Ref`/`Ldfld_Ref` (`:2716-2722`/`:2760-2764`) use `Operand3 = ReferenceOffset` and read/write `ManagedObjects[ReferenceOffset]` — i.e. `stfld`/`ldfld` of a CLR-struct field ALREADY works. Add a temporary probe (`c.f = ...; read c.f` via plain `stfld`/`ldfld`, NO `ldflda`) and confirm it PASSES on HEAD. This proves option (A) (encoding-only fix; no layout change) is correct.

## 2. JIT discriminator (the ldflda lowering — option A, D1/D2/D3)

- [x] 2.1 DUMP-GATE OQ2: confirm the chosen discriminator field is collision-free. The F-6 `NeoLdfldaInlineMarker` uses `Operand4` bit `0x1`. For F-10, pick `Operand4` bit `0x2` (a second marker bit) OR a distinct standalone field. Verify via the JIT body dump that the chosen bit/field is unused for `Ldflda` at HEAD (no collision with the F-6 marker, the `Code.Ldflda` Translate `Operand2`/`Operand3` stamps, or any optimizer site).
- [x] 2.2 In `JITCompiler.cs` `TypeSpecializeNeoOpcodes` `case Ldflda:` (`:828-846`), ADD a second condition alongside the existing in-frame-VT marker: when the source `Register2` is a HEAP IL reference type (`GetRegisterType(...) is ILType srcIl && !srcIl.IsValueType`) AND the addressed field's type is a CLR value type (re-resolve the field type via `appdomain.GetFieldOffset(token, ...)` — mirror how `Code.Ldflda` Translate gets `fieldType`; OR read a fieldType stamp the Translate case writes), stamp the F-10 discriminator on the chosen `Operand` field. Neo-only (`#if ENABLE_NEO_MODE`).
- [x] 2.3 DUMP-GATE OQ1 (D6, load-bearing): decide the mStack-index-recoverability encoding. Lean (β): keep the byref's `objIdx` = the ILTypeInstance's mStack index (so the consumer's `mStack[objIdx]` fetches the ILTypeInstance as before), and put the discriminator in the OFFSET half (a sentinel that signals "this offset is a `ManagedObjects` ref-slot index, not a `Primitives` byte offset"). The runtime `Ldflda` arm, when the F-10 discriminator is set, produces `(mStackIdx, sentinel | field.ReferenceOffset)` (or `(mStackIdx, field.ReferenceOffset)` with the discriminator carried alongside in the opcode's standalone field for the consumer to read). Verify via the JIT dump + a runtime diagnostic which encoding the consumer arm can read cleanly. If (β) is unsound, fall back to (α) bit-pack (document the bit-width assumption).
- [x] 2.4 Make the runtime `Ldflda` arm (`ILIntepreter.Neo.cs:1025-1092`) produce the F-10 byref when the discriminator is set (4th dispatch shape). The existing 3 shapes (frame-native `objIdx == -1`; in-frame-VT-flat-bytes via `NeoLdfldaInlineMarker`; heap-IL/CLR-object `objIdx >= 0` with `field.PrimitiveOffset`/`fieldHash`) stay byte-identical when the discriminator is absent.

## 3. Runtime byref consumers (D4 — scope at apply via JIT dump)

- [x] 3.1 `NeoMarshalByrefFieldToSlot` (`ILIntepreter.Neo.cs:376-...`): add a branch keyed on the F-10 discriminator (per the OQ1 encoding). When it fires AND `target is ILTypeInstance`, read/write the boxed CLR struct at `ili.ManagedObjects[refOffset]` via `ReadNeoValueType`/`WriteNeoValueType` (the Step-13b/area4 machinery). The existing ILTypeInstance-`Primitives` branch (line 379-386) and CLR-object-fieldHash branch (399+) stay byte-identical for non-discriminator byrefs.
- [x] 3.2 DUMP-GATE OQ3: dump the JIT body of the F-10 reproducer + the re-added Step-20 probes to confirm which `stind_*`/`ldind_*`/`Stobj`/`Ldobj` arms consume the F-10 byref. Ship the discriminator branch for each reached arm (mirror the `NeoMarshalByrefFieldToSlot` branch — read/write `ili.ManagedObjects[refOffset]`). For any UNREACHED width, leave the existing branch byte-identical (do NOT add speculative branches).
- [x] 3.3 For any `stind_*`/`ldind_*`/`Stobj`/`Ldobj` width the F-10 reproducer reaches but the fix does NOT cover, throw a clearly-tagged `NotImplementedException` ("neo-clrstruct-field-of-il: stind/ldind of width W through an ldflda-byref of a CLR-struct field of an IL instance") — loud, not silent corruption (the OPT-HARDEN M1 lesson).

## 4. Adversarial probes (MANDATORY — Step 17 B1 / F-MAJ-1 / F-6 lessons)

- [x] 4.1 Add the minimal F-10 reproducer probe (`NeoClrStructField_LdfldaByValDeref` or `NeoStep12_ClrStructField_LdfldaByValDeref`): IL class + CLR-struct field + `ldflda` + byref deref to a by-value CLR-method call. MUST FAIL on HEAD (`IndexOutOfRange` or silent wrong) → PASS after fix. Use `TestVector3NoBinding` (no binder, pure-primitive 3-float; exercises the reflection-fallback `CLRMethod.Invoke(byte*)` path). Route field reads through host helpers (NOT IL-side `Ldfld` on a CLR struct field — that is the separate `[NEO-IL-VT-INSTANCE-COVERAGE]` Step-6 gap).
- [x] 4.2 Add `NeoClrStructField_MultipleFields` (IL class with TWO CLR-struct fields; `ldflda` each; both read through their byrefs; assert each returns its own value — no cross-clobber).
- [x] 4.3 Add `NeoClrStructField_StructWithRefField` (IL class with a CLR-struct field that itself has a reference-type field — the `TaskAwaiter` shape; verify the boxed struct's reference field survives the byref round-trip). Use a host-side struct with a ref field, or `TestVector3WithRef` if one exists in the test framework (else add one to `ILRuntimeTestBase`).
- [x] 4.4 Add `NeoClrStructField_StfldLdfld_Regression` (IL class + CLR-struct field accessed via plain `stfld`/`ldfld`, NO `ldflda` — the regression guard proving the existing `Stfld_Ref`/`Ldfld_Ref` path stays correct).
- [x] 4.5 Add `NeoClrStructField_ByValAfterLdfldaDeref` (the byref-then-deref-to-by-value-param shape — the Step-20 builder-byref → redirect copy site; the load-bearing Step-20 shape).
- [x] 4.6 Add `NeoClrStructField_OtherFieldTypes_Regression` (IL classes with IL-primitive fields, IL-VT fields, CLR-ref fields, CLR-object fields — assert ALL stay byte-identical; the discriminator fires ONLY for the CLR-struct-field-of-IL shape).
- [x] 4.7 Add `NeoClrStructField_RegisterReuseEscape` (the Step-17-B1 class: an `ldflda`-produced byref whose dest register is reused by an intervening foldable, then the byref is read — assert no silent corruption; the F-10 discriminator does NOT perturb the `addrAlias` COEXIST gate).
- [x] 4.8 Confirm EVERY new probe FAILS on HEAD (stash-toggle the fix) → PASSES after the fix. A probe that PASSES on HEAD is either a regression guard (label it so) or a non-load-bearing probe (drop or rework). Use the DivideByZero-assertion pattern (`int _ = 1/0` on a wrong result) — no `throw new` / `[ExpectedException]` (neither exists in the harness).

## 5. Verification + ship

- [x] 5.1 Full `NeoStep` smoke stays green (186/186 baseline + new probes; 0 failed). Rebuild TestCases `--no-incremental` after every test-source edit (incremental hash hit can silently run the old DLL — the earned gotcha).
- [x] 5.2 Legacy-neutral stash-toggle: build plain `Debug` + `useRegister=true`, run the relevant filter (the F-10 probes are Neo-specific; assert they either pass on Legacy too OR the Neo-specific failure mode is documented). Confirm NO shared-engine edit beyond `ILType.cs` (which is UNCHANGED — option A) → Legacy-neutral by construction. The 7 standing pre-existing Legacy NeoStep failures are unrelated.
- [x] 5.3 Confirm the Step 20 async TC1/TC7 still pass (the `Task<int>` sync path that passed by layout accident — the fix must not break it; the byref encoding for the builder field now resolves correctly to `ManagedObjects[ReferenceOffset]`).
- [x] 5.4 (Optional, scope-permitting) Re-add a SUBSET of the trimmed Step-20 probes (TC2 SyncTask non-generic, TC3 SyncValueTaskOfT) to `TestCases/NeoStep20Test.cs` to prove the F-10 fix unblocks them. If the full re-add is large, defer to `neo-step20-async` resume — the F-10 reproducer probes (4.1-4.7) are the load-bearing proof.
- [ ] 5.5 Write `ship-log.md` (verification evidence + review verdict + delivered/deferred scope + the OQ1/OQ2/OQ3 dump-gated resolutions + any accepted-known edges). *(Shipper's job at archive.)*
- [ ] 5.6 Update `.trae/documents/neo-deferred-items.md` (F-10 row §2 + §3 entry → RESOLVED, with the (A)-vs-(B) decision + the dump evidence + the encoding chosen). *(Shipper's job at archive.)* The `planning-context.md` `## Findings -- neo-clrstruct-field-of-il (apply)` durable record WAS appended by the implementer.
- [ ] 5.7 *(LEAD's job -- implementer does NOT commit/push.)* Stage PRECISELY (the change's source files + test + `openspec/` artifacts + the deferred-items/portfolio doc updates); EXCLUDE `.pdb`/`.gitignore`/`nuget.config`/`.claude`/`.vscode`/`CLAUDE.md` churn. Commit message `Neo clrstruct-field-of-il:` + the topic. End with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. `git push origin features/object-model-overhaul`.
