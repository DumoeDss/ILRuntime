# neo-peephole-pass

D-PEEP TRUE COMPLETION: the box T; isinst U peephole fusion -- a new ADDITIVE optimizer pass (adjacent-opcode pattern-match + liveness + a fused opcode on a standalone OpCodeR field). Scope-aware: is the fusion worth a new pass, or close as accepted-known optimization (box;isinst works un-fused)?
