# neo-debugger-neo-frame

TRUE COMPLETION: Neo debugger variable inspection -- DebugService reads the Neo frame (byte* frameBase + AutoList mStack + CompiledFrame.LocalInfos) for GetThisInfo/GetLocalVariableInfo (currently 'not supported yet').
