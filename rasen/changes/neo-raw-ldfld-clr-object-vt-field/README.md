# neo-raw-ldfld-clr-object-vt-field

Neo: raw Ldfld READ sibling of child-27 (x = obj.Struct.value) -- silent flat-bytes-reinterpret corruption; needs a JIT marker (child-24 lineage) + child-27's hash-encoding read
