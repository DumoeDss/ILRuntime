# neo-raw-ldfld-array-element

Neo: raw Ldfld on a CLR-struct array element (clrStructArray[i].field read) -- child-19 deferred the Stfld counterpart; re-audit whether the runtime Array-detection approach extends to Ldfld without a JIT marker
