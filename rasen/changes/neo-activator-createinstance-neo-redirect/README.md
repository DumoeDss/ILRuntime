# neo-activator-createinstance-neo-redirect

Neo: register CLRRedirections.CreateInstance/2/3 on RedirectMapNeo (Activator.CreateInstance on IL types falls through to broken autogen stub -> MissingMethodException; same defect class as child-6 InitializeArrayNeo)
