# neo-float-arith-residual

wave2: arg1+1.2f float arithmetic bug (InheritanceTest07/16, 3E-45 garbage after typed-opcode fix). Conv_R4 seeded but a float producer still corrupts. re-audit + fix + full-smoke 65->lower
