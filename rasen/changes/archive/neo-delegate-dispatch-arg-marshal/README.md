# neo-delegate-dispatch-arg-marshal

wave2 D1: delegate-dispatch arg-marshal (3 tests single-root, DelegateExtTest01/02 + DelegateTest01). bound obj not passed as param0 to static extension-method target. HIGH-RISK (Step-19 delegate). re-audit + fix + full-smoke 31->lower
