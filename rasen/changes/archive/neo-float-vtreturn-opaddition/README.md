# neo-float-vtreturn-opaddition

Neo: re-audit the float-ctor/op_Addition VT-return gap surfaced by child-26 (new TestVector3(float,float,float) yields zero / op_Addition float VT-return doesn't write back). Diagnose-first -- the ctor-yields-zero claim is suspicious
