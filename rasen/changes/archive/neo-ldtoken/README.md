# neo-ldtoken

Implement ldtoken in ExecuteNeo: push RuntimeTypeHandle/RuntimeMethodHandle/RuntimeFieldHandle from a metadata token (typeof()/reflection). ~60 full-smoke hits, the dominant named missing opcode.
