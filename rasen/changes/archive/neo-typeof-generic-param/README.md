# neo-typeof-generic-param

wave2: typeof(<genericparam>) mis-resolution (TestGenericMethod2, possibly broader). resolves BOTH generic params to int (B=double->int). re-audit + fix + full-smoke 20->lower
