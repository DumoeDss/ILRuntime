# neo-nested-ldflda-byref

wave2 D4: nested ldflda-on-byref (a.Struct.value += N, 3 tests, child-24/29 lineage deferred). inner ldflda on a byref-to-boxed-struct. re-audit + fix + full-smoke 32->lower
