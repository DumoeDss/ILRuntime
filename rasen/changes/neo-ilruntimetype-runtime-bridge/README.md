# neo-ilruntimetype-runtime-bridge

wave2 C12: ILRuntimeType passed to Delegate.CreateDelegate/Enum APIs that need a runtime Type -> 'Type must be a runtime Type' (10 failures). re-audit + bridge + full-smoke verify
