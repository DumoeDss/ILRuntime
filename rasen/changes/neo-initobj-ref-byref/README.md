# neo-initobj-ref-byref

wave2: initobj on ref-T through byref (default(T) on ref T). CIL-producer scan approach (JIT emission case Code.Initobj, follow ins.Previous, mirrors child-24/29). re-audit + fix + full-smoke 21->lower
