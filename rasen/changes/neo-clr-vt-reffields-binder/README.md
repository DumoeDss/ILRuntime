# neo-clr-vt-reffields-binder

CLR value type with reference fields and no ValueTypeBinder (Step 13b) throws (~18 hits, incl. RuntimeHelpers.InitializeArray RuntimeFieldHandle). Register a binder or handle the marshal. Probes.
